// Web prototype: no-op stub. Preserves the API for a future native build.
export const triggerHaptic = (
  _style: "light" | "medium" | "heavy" | "success" | "warning" | "error" = "light"
) => {
  // no-op on web
};
